// ============================================================================
// App version label — bump this when shipping notable changes so the user can
// instantly tell which build they're looking at (shown in Settings home,
// alongside the runtime build id from /api/version which changes on every
// production deploy automatically).
// ============================================================================
export const APP_VERSION = 'v2.5'
